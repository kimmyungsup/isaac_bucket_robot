[project]
name = "isaac_bucket_robot"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "isaacsim[all,extscache]==5.1.0.0",
    "pyserial",
    "numpy",
    "matplotlib",
]

[tool.uv]
extra-index-url = ["https://pypi.nvidia.com"]
